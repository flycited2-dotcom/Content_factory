# Хендофф 2026-07-09 (вечер) — /auto, опт Бриза, свой товар, цены/наценки

Марафонная сессия с владельцем (десктоп DESKTOP-A59E7AI, деплой ключом
`splithub_deploy`). Всё в проде: **446 тестов зелёные**, cf-bot перезапущен.
Читать после [HANDOFF-2026-07-07-EVENING](HANDOFF-2026-07-07-EVENING.md).

## Сделано сегодня (все коммиты в master, запушено)

1. **Выключатель/редактор авто-контента `/auto`** (задачи 2+5 бэклога):
   флаг в settings state-БД (нет записи = ВЫКЛ), `maybe_materialize` отменяет
   pending `auto-*` при выкл; кнопки вкл/выкл + 🕐/🔢/📦/↩️; `/auto times|count|
   cats|reset` (override поверх yaml, id от crc32 содержимого); категории
   СЛОВАМИ (справочник из catalog_category, `resolve_cats`); после правки —
   итоговый статус. Спек: [PLAN-auto-switch.md](PLAN-auto-switch.md).
2. **Опт Бриза** (задача 3): в БД сайта у Бриза РОЗНИЦА; опт только в
   `/leftoversnew/`. Порт `fetch_breez_base_by_nc` завершён, `live_base_lookup`
   подключён во ВСЕ раннеры (scheduler/cards/channel_sync — везде была заглушка
   `lambda nc: None`). 142 breeze-позиции подешевели (~20%). Синк цен прогнан
   владельцем: 12 опубликованных постов обновлено.
3. **Наценки из бота**: `/markup breeze -3`, `/markup * 8` (overrides в
   settings поверх yaml, `pricing/overrides.py`), `/markup` — обзор.
4. **«➕ Свой товар»** в /task: название → цена → время → фото/УТП; force_reply
   с плейсхолдерами (владелец принимал кнопку «❌ Отмена» за ошибку); работает
   с любого шага; «назад»: 📎 Фото заново / 📝 УТП заново на подтверждении.
5. **💹 Наценка партии** на подтверждении визарда: ±% на все выбранные позиции.
6. **💰 Изменить цену для СЕРИЙ**: превью планировщика на общем `preview_markup`
   (код через OrderLinks — длинные ключи больше не в текстовый фолбэк); для
   серий линейка сдвигается пропорционально (`scale_prices_in_caption`, …90).
7. **Визард-фиксы**: команды проходят СКВОЗЬ визард (бот жрал /auto «не понял
   номера»); кнопка ❌ Отмена на шаге списка; crash-loop на Подтвердить (фото
   отн. путём → FileNotFoundError → 31 рестарт: resolve + `_wizard_safe` — визард
   больше не роняет main()); submit_card ДО add_items (иначе падение сабмита
   роняло товар в research с чужим фото).
8. **Поиск**: «морозильные камеры» больше не выдаёт холодильники (имён-матчи
   вытесняют секционные; алиас «камера ≈ ларь» в search_aliases.yaml).
9. **Фотоагент** (репо agent_convert_foto_rituailb2b2, main, 386685e):
   промпт research_image изолирован от памяти проекта ChatGPT — карточка
   морозилки M514 получила фото «RDF-260DD», которого НЕТ в очереди (память
   проекта). Дорожки desktop-a1/a2 перезапущены с новым кодом.

## Диагнозы (проверено фактами, не чинилось)

- **Дубли карточек** (владелец: «льются по кругу») — НЕ цикл: ретраи после
  падений агента (таймауты/смена DOM/CDP; «Smartline DC» 7/10 failed),
  MAX_TRIES=3. В превью/канале дублей нет (0 повторов, ключи уникальны).
- **created_at в очереди агента** — ТЕКСТ, не unixepoch: сравнения с числом
  дают «всю историю» (грабля моей диагностики).

## Осталось владельцу / следующей сессии

- **Ларь Hyundai CH1002**: карточка ушла с ChatGPT-фото (crash-петля успела
  загнать товар в research-путь). Пересабмит с фото владельца НЕ выполнен
  (авторежим блокировал прод-write). Быстрый путь: «Свой товар» заново с тем
  же названием — перепишет card_job. Скрипт-пересабмит: scratchpad/resubmit.py.
- **ChatGPT-проекты**: отключить «ссылаться на другие чаты проекта» в настройках
  research/карточных проектов (2 клика в UI) — надёжнее промпта.
- **Ноутбук**: `git pull` в репо агента + перезапуск (перезагрузка) — фикс
  промпта; content-factory на ноуте — `git pull` (всё в GitHub master).
- **Разметка мощностей**: `config/power_map.yaml` ждёт ручного заполнения
  btu_true (владелец просил «файлик, я напишу что к чему» — он уже есть).
- Задачи 1 (серийная группировка прайса) и 6 (сток-бот в /task) бэклога 07-07 —
  не начаты. Пагинация /find (задача 5) — не начата.

## Грабли сегодняшние (не повторять)

- **Чужие стэши в репо агента**: `git stash pop` вслепую вытащил чужой WIP →
  конфликты; восстановление потребовало ОК владельца. Сравнивать версии через
  `git show`/worktree (память: agent-repo-shared-stash-danger).
- **Авторежим Claude Code** блокирует прод-write (systemctl start, UPDATE БД)
  даже после устного ОК — иногда нужен переход в обычный permission mode.
- Telegram-очередь бота: падение main() до сдвига offset = crash-loop на одном
  апдейте. Любые новые обработчики — только через `_wizard_safe`-паттерн.
- Секции прайсов смешанные («Холодильники и морозильные камеры») — матч по
  секции даёт мусор, имён-матчи приоритетны.

## Доступы/деплой (без изменений)

VPS 213.109.202.45, с этой машины — ключ `~/.ssh/splithub_deploy`; деплой
tar+ssh (`cat > /tmp/x.tgz` + `tar -xzf`, scp не работает), рестарт cf-bot;
таймеры (scheduler/cards/excel/sync) подхватывают код сами. Прод-write —
только по явному ОК владельца в каждой сессии.
