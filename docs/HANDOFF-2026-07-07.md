# Хендофф 2026-07-07 — /task v2, источники с наценками, волна апгрейда

Читать первым в новой сессии (с памятью). Предыдущие: [HANDOFF-2026-07-06-EVENING.md](HANDOFF-2026-07-06-EVENING.md),
[HANDOFF-MULTI-MACHINE.md](HANDOFF-MULTI-MACHINE.md).
**Спека-источник правды по дальнейшим работам:**
[superpowers/specs/2026-07-07-factory-upgrade-wave.md](superpowers/specs/2026-07-07-factory-upgrade-wave.md).

## В ПРОДЕ за 07-07 (все тесты зелёные — 381; всё задеплоено, cf-bot перезапущен)

1. **Ночная волна фидбека** (см. дополнение в HANDOFF-2026-07-06-EVENING):
   фикс 404-цикла агента («тишина»), кнопка «💰 Изменить цену» на превью,
   читаемые /status (done свёрнуты) и /excel (ошибки 1-й строкой, разделители).
2. **/task v2** (a6ffa4c): кнопки категорий из прайсов → нумерованный автосписок
   (до 30) → номера «1 3 5»/«все» → время «🚀 Сейчас» / «завтра 9:00»
   (parse_due_at) → подтверждение. Расписание = excel_items.due_at, тик не берёт
   до срока (ExcelStore.by_status). Фото/УТП-override только в «сейчас».
   Многострочный ввод на шаге категории = свой список моделей (старый путь).
3. **Источники с наценками** (4d9936d): прислал .xlsx → источник (механика
   manual__* от параллельной сессии 07-05) → бот СЛЕДОМ спрашивает наценку
   (+5/-7/0, ForceReply → «/markup <слот> <N>»). prices/markups.json,
   применение в load_price_slots (единая точка). /sources — список; /markup —
   смена. Глобальный EXCEL_MARKUP_PCT не тронут (0). Меню setMyCommands обновлено.

## ⚠️ Не проверено живыми тапами владельца
/task v2 целиком (кнопки категорий, автосписок, расписание), /sources, /markup,
авто-вопрос наценки, 💰-кнопка (на новых превью), новые форматы /status и /excel.

## Решения владельца по 12 идеям (детали в спеке!)
- ✅ Автопилот наполнения — ОБЯЗАТЕЛЬНО, с товарными ГРУППАМИ (КБТ=холодильники+
  морозилки+лари+стиралки+ТВ; МБТ; сплиты; бойлеры), доли настраиваемые,
  «10 КБТ» = разнообразный микс, НЕ одинаковые позиции.
- ❌ API-генерация картинок — ЗАПРЕЩЕНА НАВСЕГДА (владелец проверял: качество
  «прям в минус» против ChatGPT-проектов). НЕ предлагать.
- ✅ Авто-диф прайсов, дайджест 21:00, /health (с реальным CDP-attach),
  умные цены (округление, плашка «↓»), приоритеты очереди, /leads-CRM.
- ❌ Мультиканальность — отложена («слишком сложно пока»).
- Фидбек: владелец ждёт идей «на опережение» — готовить после появления
  телеметрии (дайджест+health), на реальных цифрах кликов/лидов.

## Следующий заход (порядок по спеке)
1. Товарные группы (config.yaml groups: стем-паттерны разделов → КБТ/МБТ/…)
   + group_of(item). 2. Автопилот наполнения (норма постов/день, доли групп,
   буфер превью, разнообразие, дневной cap генераций). 3. Приоритеты
   (excel_items.priority, /task обгоняет автопилот). Дальше волна 3 из спеки.

## Инфраструктура (без изменений за день)
Обе дорожки ноута работают (акк1+акк2 параллельно); десктоп НЕ обновлён
(git checkout main && git pull — на владельце); переключатель «Только
ноут/десктоп» в боте фотоагента; stop_local.bat на ноуте перед выключением.
Репо агента: main (9bfbbd6 — фикс 404). Грабля зомби-Chrome после ребута —
в памяти project-agent-lanes (TODO: CDP-attach проверка в вотчдоге).
