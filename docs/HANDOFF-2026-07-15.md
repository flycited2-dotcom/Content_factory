# Хендофф «Контент-завода» — 2026-07-14/15 (desktop)

Читать первым в новой сессии (вместе с памятью). Предыдущий: [HANDOFF-2026-07-09-EVENING.md](HANDOFF-2026-07-09-EVENING.md).

## 1. Бренды на карточках — «HOMELINE RDF-260DD» (диагноз + фикс)

Все kbt-миксеры выходили с брендом/моделью ЭТАЛОНА (эталоны kbt — холодильник и
стиралка HOMELINE). Корень: промпт велит «возьми бренд из названия товара в этом
сообщении», но excel-конвейер слал в specs только «✓ Мощность…» без названия, а
brand/model задачи агент клал только в имя файла. Без текста модель читала бренд
с Фото 3 (кондиционеры с логотипом на корпусе выживали) или списывала с эталона.

**Фикс (репо агента, e726366):** `Mode.render_prompt(specs, brand, model)` — первой
строкой блока {{SPECS}} идёт «НАЗВАНИЕ ТОВАРА: {brand} {model}». Чинит kbt/mcp/
conditioner разом. Проверено вживую: Ballu/Edisson/Candy вышли с верными брендами.

Побочно: 10 опубликованных миксер-постов снесены из канала (решение владельца,
вариант 2), позиции перегенерированы. Файлы карточек на диске могут отличаться от
опубликованных версий (перезапись при перегенерации) — сверять только с каналом.

## 2. Фото товаров: research-фото = рисунок ИИ → ручные фото ответом на превью

«Позорище с бойлерами Ballu»: у прайс-позиций нет фото, research-шаг РИСУЕТ товар
по названию — для техники с фирменным дизайном выходит непохожий генерик.

**Фича (владелец выбрал вариант 1): фото ответом на превью** (content-factory
4ede092 + фиксы 6dbb8d3 личка/группа, 54d7dd1 абсолютный путь; ЗАДЕПЛОЕНО):
- владелец отвечает на превью реальным фото → bot/manual_photo.py берёт код из
  reply_markup превью (approve:<code> → links.key_for), кладёт фото в
  state/manual_photos/ и в research_cache (source='manual' — research НЕ
  перезапишет), позиция уходит на переген штатной regen-логикой;
- проверено end-to-end: Edisson ER 100 V и Ballu BWH/S 80 Shell — карточки с
  реальными товарами 1-в-1 (нюанс: стиль может дрейфовать светлый/тёмный).
- фото без reply — как раньше, в визард.

TODO-варианты 2–4 (не делались): автоисточник фото поставщика, пометка «⚠️ фото
ИИ» на превью, запрет research-фото по категориям. Партия ~161 превью вся с
ИИ-фото — фильтровать глазами, докидывать реальные фото ответом.

## 3. Мощности: power_map размечен ПОЛНОСТЬЮ (все 4 поставщика)

Механизм `sizing.size_for()` (b6ea7bc) + разметка владельца в
[config/power_map.yaml](../config/power_map.yaml): breeze 29, daichi 12,
rusklimat 11, jac 29 кодов (bcec17f, a685f2c, 89416b3; ЗАДЕПЛОЕНО, cf-bot
перезапущен). btu_calc больше не источник для размеченных кодов; у jac btu_calc
НЕТ вовсе — map единственный источник. Мусор (пульты, зимние комплекты WS-RAC,
наружные блоки, годы _24Y, ревизии .D05) размечен нулями — составные названия
jac решаются именно нулями. Новый код → строка в yaml → tar-деплой + restart.

## 4. Опт по кондиционерам (факты)

Покрытие 100%, но свежесть: daichi — живой синк; breeze — хвост с 10 мая;
rusklimat — ПРОТУХ (18–24 мая); jac json от 2 июля. Живой Breez-опт
(/leftoversnew/, в БД у Бриза РОЗНИЦА!) реализован с ноута (f30391b, bd82d66) +
наценки источников /markup (bdcb48f). TODO: реанимировать синки rusklimat/jac.

## 5. Доступ и деплой с десктопа

- SSH: ключ `id_ritualb2b_admin` → root@213.109.202.45, разрешён владельцем в
  .claude/settings.local.json (агентский id_ritualb2b_agent — только туннель).
- Деплой content-factory: НЕ git на VPS — tar по ssh в /opt/content-factory +
  `systemctl restart cf-bot` (таймерные cf-* подхватывают сами). Бэкапы в /tmp.
- sqlite3-CLI на VPS нет — запросы питоном через stdin.

## 6. Не доделано / открытое

- **Эталоны kbt с нейтральным брендом** — начато (зоны размечены: лого
  HOMELINE + плашка модели + мелкий лого на приборе, оба эталона), НЕ доделано.
  Второй слой защиты от переноса бренда. mcp-эталон CARVER — риск ниже.
- Сеть до VPS моргает (ssh-обрывы) — деплой-скрипты с ретраями.
- result_sender: пустой output зацикливает отправку (старый TODO, не чинился).
