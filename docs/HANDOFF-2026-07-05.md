# Хендофф «Контент-завода» — 2026-07-05

Читать первым в новой сессии (вместе с памятью). Предыдущий: [HANDOFF-2026-07-04.md](HANDOFF-2026-07-04.md).

## Диагностика логов (запрос владельца «посмотри логи бота»)

1. **У `cf-bot` не было НИКАКИХ логов от самого процесса** — только системные
   старт/стоп строки. Причина: stdout буферизуется под systemd (не терминал),
   единственный `print()` в коде никогда не сбрасывался. Фикс:
   `PYTHONUNBUFFERED=1` в `cf-bot.service` (и на VPS, и в шаблоне
   `deploy/cf-bot.service` — коммит `a319a0d`). Проверено: после рестарта строка
   `bot: long-poll запущен` появилась в `journalctl`.
2. **Генератор карточек на ноуте был остановлен вчера в 17:57** — точная причина
   найдена в `remote_agent.log`: модалка rate-limit ChatGPT заблокировала клики.
   Судя по всему, кто-то (владелец?) через ~13 сек нажал «Стоп» в боте фотоагента,
   `agent_state.txt` → `stopped`. Вотчдог корректно уважал стоп 19 часов — это не
   баг. По команде владельца отправлен `agent_command_laptop=start` (флаг в
   queue.db на VPS, тот же путь, что кнопка бота) — ноут поднялся: Chrome CDP
   ответил, `remote_agent.py` запущен, лог подтверждает.
3. **⚠️ Очередь всё это время не стояла на месте** — на момент проверки (07-05
   12:41) в работе была задача 506, heartbeat свежий, хотя ноут был остановлен.
   Значит десктоп (со старым кодом, не проверяет лиз — Task 7 из предыдущих
   хендоффов) продолжает грызть очередь самостоятельно. Не чинил — это тот же
   известный конфликт, требует физического доступа владельца к десктопу.

## Обновление ~13:05: зависшая задача 506 (владелец «завис агент»)

Задача 506 (SHUFT TOR, серия НС-1688398) висела в `processing` ~30 мин без
движения — не на этом ноуте (в его логе о ней вообще нет упоминаний), похоже
захвачена десктопом и там зависла. Владелец подтвердил сброс: `POST /api/fail/506`
через официальный эндпоинт (не руками в БД). В `card_jobs.db` у серии `tries=2`
из MAX_TRIES=3 — на следующем тике `cf-cards.timer` (каждые 30 мин) переотправится
автоматически, последняя попытка. Раз ноут сейчас рабочий — есть шанс дойдёт до конца.

## Статус на 07-05 ~12:50

- `cf-bot`: активен, логи теперь видны.
- Ноут-агент: только что поднят командой `start`, генерирует.
- Десктоп: предположительно всё ещё активен параллельно (не проверен физически).
- Очередь фотоагента: done=313, failed=156 (в основном вчерашние rate-limit
  и утренние connect_over_cdp — оба инцидента объяснены, не активны прямо сейчас).

## Дальше (без изменений от 07-04)

Актуальный бэклог — см. [HANDOFF-2026-07-04.md](HANDOFF-2026-07-04.md#дальше-бэклог-по-приоритету-владельца):
Task 7 (десктоп, нужен физический доступ — теперь особенно важно из-за
конфликта с ноутом), лид «в один клик», категория+фильтр в /task, идеи low-priority.
