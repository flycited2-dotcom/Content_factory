# Хендофф «Контент-завода» — 2026-07-04 (после мега-дня 07-03)

Читать первым в новой сессии (вместе с памятью). Предыдущие: [HANDOFF-2026-07-03.md](HANDOFF-2026-07-03.md),
[HANDOFF-2026-07-01.md](HANDOFF-2026-07-01.md). Инструкция владельца: [../ИНСТРУКЦИЯ.md](../ИНСТРУКЦИЯ.md).
Спеки: [2026-07-02-full-automation](superpowers/specs/2026-07-02-full-automation-design.md),
[2026-07-03-wave1](superpowers/specs/2026-07-03-wave1-improvements-design.md).

## Что сделано за 2026-07-03 (весь день, всё В ПРОДЕ и проверено владельцем)

1. **Автомат климатики**: авто-задачи включались, окна работали; анти-дубль расширен
   (rejected/awaiting не повторяются). Серийная подпись: «💰 от X ₽» + линейка
   «▫️ 07 · цена · шт.» (render_caption member_prices). Цена всегда отдельной строкой с 💰.
2. **Research отлажен окончательно**: двухшаговый (УТП текстом → изображение отдельным
   сообщением, prompts/research{,_image}.txt). Пустые ответы = лимиты ChatGPT.
3. **Живой канал (волна 1а)**: cf-channel-sync.timer 08:30; первый прогон пометил
   ALFACOOL APUS «⛔ ПРОДАНО» в СплитХаб.ру.
4. **Кнопка «📩 Заказать» (1б)**: url-кнопка на новых постах → /start ord_<code> →
   лид владельцу + журнал leads. Первый лид пойман (сам владелец).
5. **Excel-источник (подпроект 3 + кэш УТП 1в)**: бот принимает .xlsx; `/make N категория
   brand=K brand=*` (поиск без падежей, фраза сужает), `/find` (нумерованный список 15),
   `/pick 1 3 5`, `/excel` (статус). Конвейер excel_run (cf-excel.timer */10 мин):
   new→research(кэш!)→card(kbt)→preview(✅/❌/🔄, цена из прайса, EXCEL_MARKUP_PCT дефолт 0).
   ТРИ формата прайсов: таблица (БытТехОпт), 1С-карточки (ИП Аксёнов, 848 поз.),
   1С-иерархия (почта 1С, 155 поз.).
6. **Почта-источник**: cf-mail.timer */30 мин, IMAP flycited@gmail.com (app-password в .env),
   письма от info@simfer.com.ru → вложение .xlsx = рабочий прайс + сводка владельцу.
7. **Прогнано вживую**: 3 генератора CARVER и 7 чайников из прайсов → превью → публикации
   (message_id 408-410+) с кнопкой «Заказать».
8. **Алерты конвейера** (требование владельца «чтобы не было тишины»): ⚠️ превью не
   отправилось (с причиной), ❌ выпал из конвейера (список+причины), 🏁 партия обработана.
9. **Вотчдог починен**: самовосстановление работало НИКОГДА (API отдаёт строку "none");
   адресные флаги agent_command_<worker> (конфликт ноут/десктоп устранён).

## Грабли дня (все с фиксами — НЕ повторять)

- `<артикулы>` в названиях товаров + parse_mode=HTML → Telegram молча отклоняет посты →
  html.escape в build_preview_caption (excel_run).
- Длинные excel-ключи не влезают в callback_data 64 байта → короткий код (OrderLinks.code_for)
  + resolve_callback_data в боте.
- Бот не видел ОТРЕДАКТИРОВАННЫЕ сообщения → edited_message тоже обрабатывается.
- Лимиты ChatGPT Plus (изображения): пауза + троттлинг (ретраи research 60с, между
  задачами 45с). Пересборку бэклога сплитов гонять умеренно.
- Каталоги input/output на VPS иногда исчезают (виновник не найден) → защитный mkdir в vps_api.
- Снятые (cancelled) задачи очереди агента навсегда висли pending в card_jobs store и
  съедали бюджет max_pending (submitted=2 вместо 10) → run_once сверяет cancelled как
  failed, серии переотправляются (фикс 69cfd99, задеплоен 07-03 вечером).
- **/make молча угадывал количество из середины текста**: владелец прислал список
  моделей («…Stinol WSTU 410 C…») без синтаксиса `/make N категория` — число «410»
  подхватилось как count, категория молча стала одним словом «стиральная» → «выбрано 2
  (запрошено 410)» вместо честной ошибки. Фикс: count строго первым токеном, иначе
  ValueError с подсказкой (коммит 5d22ab6).
- **Почта молча затирала ручной прайс владельца**: единственный latest.xlsx каждые
  30 мин (cf-mail) перезаписывался почтовым прайсом — позиции из ручной загрузки
  переставали находиться в /make и /find. Фикс: два слота — manual.xlsx (свой,
  приоритет) + mail.xlsx (почта), load_price_slots ищет в обоих (коммит 5d22ab6).
  На VPS смигрировано: старый latest.xlsx (почта, 155 поз.) → mail.xlsx, прайс
  ИП Аксёнов (848 поз., последний ручной загруз) → manual.xlsx. cf-bot перезапущен.
  ⚠️ Не хватает: 5 моделей, которые владелец реально просил (LG F1096ND3, Midea
  MF200W90WBS/S-RU, Stinol WSTU 410 C, Midea MDRB457FGF01ID, Sakura SA-7055W) — их
  нет ни в mail.xlsx, ни в manual.xlsx, они есть только в локальном
  price_test/БытТехОпт_20260702.xlsx (1861 поз., НЕ загружен на сервер — владелец
  решил не заливать руками, см. ниже про источник-канал).
- **Backlog (по решению владельца 2026-07-03 веч.)**: владелец упомянул канал Telegram
  (chat_id -1001929222037), куда каждый день прилетает актуальный прайс, и бот
  Никита/Sendpr1ce_bot там уже админ. Идея — авто-забор прайса из этого канала
  (channel_post с .xlsx-документом) вместо ручной загрузки/почты — как ТРЕТИЙ слот
  прайсов. Владелец попросил сделать это ОТДЕЛЬНОЙ задачей позже, не в этой сессии.
- У ДЕСКТОПА GENERATION_TIMEOUT_SEC≈180-210с — сложные картинки не успевают (задачи 288,
  307 падали) → при Task 7 поставить 300.

## Состояние на утро 2026-07-04

- **Таймеры**: cf-scheduler ✅ (5 мин), cf-mail ✅ (30 мин), cf-excel ✅ (10 мин),
  cf-channel-sync ✅ (08:30), **cf-cards ✅ ВКЛЮЧЁН 07-03 вечером** (владелец оплатил
  подписку ChatGPT и дал команду; тик 30 мин, PER_RUN=10/MAX_PENDING=12 из .env).
- **auto_tasks в config/config.yaml включены** (окна 10/12/10) — утром в 9:00 пойдут превью
  кондиционеров ИЗ ГОТОВЫХ карточек; новые карточки не генерятся, пока cf-cards выключен.
  43 серии бэклога ждут пересборки в mcp.
- **Агенты**: ноут — новый код (research/caps/троттлинг), десктоп — СТАРЫЙ (Task 7:
  git pull ветки claude/conditioner-approval-pipeline, .env WORKER_ID=desktop/PRIORITY=1/
  GENERATION_TIMEOUT_SEC=300; идея владельца: второй аккаунт ChatGPT на десктоп = удвоение лимитов).
- excel_items: 3 генератора + 7 чайников в preview/published; research_cache наполняется.

## ⚠️ ИНЦИДЕНТ 2026-07-04 утро: конвейер карточек стоит (найдено, НЕ починено)

Проверял утром "прогоним генерацию" — нашёл двойной сбой одновременно:
1. **Ноутбук**: `agent_watchdog.py`/`remote_agent.py` зависли 07-03 в 23:26-23:27 (оба лога
   обрываются в ту же секунду — процесс убит грубо). Задача планировщика
   `RitualB2B_Watchdog_Laptop` считает предыдущий запуск «всё ещё выполняется»
   (LastTaskResult 0xFFFFFFFF) и с тех пор пропускает каждый 5-мин триггер —
   **139 пропусков** к утру. Никакой локальный процесс агента/вотчдога на ноуте
   сейчас не запущен. Фикс: `Stop-ScheduledTask`+`Start-ScheduledTask` — владелец
   попросил НЕ трогать, разберётся сам.
2. **Десктоп (вероятно)**: с 00:12 до утра КАЖДЫЕ ~10 мин что-то берёт задачу из очереди
   на VPS и роняет с `BrowserType.connect_over_cdp: Timeout 180000ms exceeded` (Chrome
   не отвечает после подключения вебсокета — завис/не поднялся). Это не ноут (он мёртв,
   см. п.1); agent-command-поллинг при этом идёт каждые 15с — похоже на десктоп со
   старым кодом (не проверяет лиз, значит перехватывает очередь). **За ночь так провалился
   ВЕСЬ бэклог пересборки сплитов**, включённый вечером 07-03 (0 успешных карточек с 23:26).
   Владелец: «десктоп не рядом, выключить не могу сейчас» — ждём его физического доступа
   (см. п.3 «Task 7» ниже — теперь ещё срочнее: там же старый Chrome/агент нужно почистить).

## Дальше (бэклог по приоритету владельца)

0. **СНАЧАЛА** инцидент выше: перезапустить ноут-вотчдог (Stop/Start-ScheduledTask
   RitualB2B_Watchdog_Laptop) и разобраться с десктопом (выключить или обновить —
   см. п.3), иначе cf-cards продолжит впустую кормить сломанный воркер.
1. Лид «в один клик» (ссылка на клиента + /leads) и вечерний отчёт завода — быстрые.
2. ~~Включение cf-cards + пересборка бэклога сплитов~~ СДЕЛАНО 07-03 вечером: 19 старых
   conditioner-карточек перенесены в /opt/oasis/staticfiles/cf-cards-bak-conditioner-20260703,
   серии перегенерятся в mcp автоматически (у них нет записей в store).
3. Task 7 (десктоп) при физическом доступе владельца.
4. Из идей: дайджесты/🔥-приоритеты, мультиканальность (avito-bridge!), PDF-прайсы,
   OCR фото-прайсов, авто-QC карточек, сезонность. «Пульт» — после стабилизации источников.
5. ⚠️ Владельцу: пересоздать app-password Gmail после тестов (светился в чате).
6. НОВОЕ: источник-канал прайса (chat_id -1001929222037, бот Никита/Sendpr1ce_bot —
   админ, ежедневный актуальный прайс) — третий слот наравне с manual/mail
   (load_price_slots), приём через channel_post с .xlsx-документом (как приём файла
   от владельца в bot/run.py, но для этого chat_id). Владелец попросил отдельной
   задачей — не забыть на старте следующей сессии.

## Операционка (без изменений)

VPS 213.109.202.45 (ключ ~/.ssh/climat_simf_deploy), деплой tar+ssh; фотоагент
/root/ritualb2b (файлы по ssh cat >, бэкапы .bak-*); тесты: content-factory 269 (pytest),
агент 65 (pytest). Прод-действия — по ОК владельца. Секреты в .env, не эхоить.
