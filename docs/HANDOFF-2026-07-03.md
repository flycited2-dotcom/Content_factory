# Хендофф «Контент-завода» — 2026-07-03 (ночь после большого дня 07-02)

Читать первым в новой сессии (вместе с памятью). Предыдущий: [HANDOFF-2026-07-01.md](HANDOFF-2026-07-01.md).
Спека дня: [superpowers/specs/2026-07-02-full-automation-design.md](superpowers/specs/2026-07-02-full-automation-design.md)
(+ раздел «Дополнения владельца»). Инструкция владельца: [../ИНСТРУКЦИЯ.md](../ИНСТРУКЦИЯ.md).

## Что случилось 2026-07-02 (одним абзацем)

Одобрена и реализована спека «полный автомат»: авто-задачи из конфига + ревью-группа
«Тест_контент_завод» (-5428762212) с кнопками ✅/❌/🔄 + research-конвейер в фотоагенте
(«наименование → УТП+фото из интернета» через ChatGPT-проект Research). Всё задеплоено,
прогнано вживую: **8 постов опубликованы владельцем в «СплитХаб.ру» через ревью**, включая
первый Excel-товар (холодильник Stinol STS 167: research-УТП + kbt-карточка + цена из
прайса). Владелец оценил «на 99%». Вечером упёрлись в лимиты ChatGPT — работы остановлены
до утра.

## Состояние систем (на момент хендоффа)

- **cf-bot** — работает, умеет ✅/❌/🔄 (кнопка regen в проде; на старых превью — `/regen <key>`).
- **cf-scheduler.timer** — тикает, но `auto_tasks` в `config/config.yaml` ЗАКОММЕНТИРОВАНЫ
  (владелец подтвердил mcp-стиль — можно включать по его команде: раскомментировать + деплой).
- **cf-cards.timer** — ВЫКЛЮЧЕН (`systemctl enable --now cf-cards.timer` для включения).
  Без него 🔄-перегенерация и новые карточки НЕ пересоберутся!
- **Research**: Stinol done; **Beko BCNA306E2S (270) и Gorenje NRK6201ES4 (271) — failed
  из-за лимитов ChatGPT** (диагностика в error_text: ответ пустой). Утром: сбросить в
  pending (`UPDATE jobs SET status='pending', error_text=NULL, result_sent=0 WHERE id IN
  (270,271)`) и перезапустить — потом их kbt-карточки и превью (шаблон:
  scratchpad preview272.py прошлой сессии; цены: Beko 51 644 ₽, Gorenje 40 451 ₽).
- **Генераторы**: ноут — новый код (research, caps, троттлинг 60/45с — троттлинг
  подхватится при следующем рестарте агента); десктоп — СТАРЫЙ код, работает, research
  не получает (caps-фильтр в next-job). Task 7 (обновить десктоп) актуален.
- **Очередь агента**: пуста (5 mcp-задач отменено при чистке, бэкапы queue.db.bak-*).

## Грабли, пойманные вживую (все уже с фиксами, кроме двух)

1. Старый агент фейлит research «битой задачей» → `caps=research` в next-job (fix).
2. ChatGPT шлёт текст и картинку РАЗНЫМИ сообщениями → текст собирать со всех (fix).
3. Маркеры markdown-списков не попадают в innerText (CSS ::marker) → `<li>` собирать
   с префиксом «- » (fix).
4. Лимиты ChatGPT: пустые ответы + СМС владельцу → троттлинг 60/45с (fix), дневные
   лимиты в конфиг — TODO.
5. ⚠️ БЕЗ ФИКСА: **вотчдог ноута не реанимирует умершего агента** (агент лежал 10 мин
   при живом вотчдоге). Лечение руками: kill + флаг `agent_command=start` в queue.db.
   Разобраться в `agent_watchdog.py` (сверка состояния).
6. ⚠️ БЕЗ ФИКСА: **rejected-серии не в анти-дубле** — авто-задача выберет отклонённую
   серию снова (карточка есть → превью повторится). Решить с владельцем: reject = «навсегда»
   или «не сейчас»; фикс — published_keys ∪ rejected в build_context.
7. На VPS исчезал `/root/ritualb2b/input/` → 500 на submit-job. Причина не найдена,
   каталоги пересозданы. Если повторится — искать, кто чистит.
8. IP контейнера БД меняется после ребута VPS → `deploy/update_db_host.sh` (ExecStartPre, fix).

## Как продолжить утром (по сигналу владельца)

1. `python -m pytest -q` в обоих репо (204 + 63 зелёных).
2. Research: сбросить 270/271 (см. выше), перезапустить ноут-агента (kill + флаг start),
   мониторить; done → kbt-карточки (curl-шаблон в истории: -F mode=kbt -F specs=<утп
   -F photo=@research_NNN.png + сразу `result_sent=1`) → превью через ConfirmStore+publish_post.
3. По команде «включай автоматику»: раскомментировать auto_tasks в config/config.yaml,
   задеплоить конфиг, `systemctl enable --now cf-cards.timer`. Помнить про лимиты GPT:
   32 поста/день = много генерации; старые conditioner-карточки будут постепенно
   вытесняться только по 🔄.
4. Дальше по спеке: подпроект 3 (Excel-источник в cf-bot: приём .xlsx + `/make` +
   research_pipeline.py в content-factory), потом адаптеры источников и «пульт».

## Доступы/деплой (без изменений)

VPS 213.109.202.45 (ключ `~/.ssh/climat_simf_deploy`), деплой tar+ssh (scp не работает),
фотоагент-файлы на VPS: `/root/ritualb2b/{vps_bot.py,vps_api.py}` (cat > по ssh, бэкапы
.bak-*). Секреты в .env, в чат не эхоить. Прод-действия — по ОК владельца.
